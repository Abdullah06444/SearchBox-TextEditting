            <% if (value != "...") { %>
                <% value = value.charAt(0).toLocaleUpperCase('tr') + value.substring(1,).toLocaleLowerCase('tr'); %>
                <% if (platform == 1 && type == 1) { %>
                    <% informationRetrieval.rankedIndexHepsiburada(value.forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 1 && type == 2) { %>
                    <% informationRetrieval.positionalIndexHepsiburada(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 1 && type == 3) { %>
                    <% informationRetrieval.invertedIndexHepsiburada(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 2 && type == 1) { %>
                    <% informationRetrieval.rankedIndexAmazon(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 2 && type == 2) { %>
                    <% informationRetrieval.positionalIndexAmazon(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 2 && type == 3) { %>
                    <% informationRetrieval.invertedIndexAmazon(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 3 && type == 1) { %>
                    <% informationRetrieval.rankedIndexN11(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 3 && type == 2) { %>
                    <% informationRetrieval.positionalIndexN11(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 3 && type == 3) { %>
                    <% informationRetrieval.invertedIndexN11(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 4 && type == 1) { %>
                    <% informationRetrieval.rankedIndexCiceksepeti(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 4 && type == 2) { %>
                    <% informationRetrieval.positionalIndexCiceksepeti(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 4 && type == 3) { %>
                    <% informationRetrieval.invertedIndexCiceksepeti(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 5 && type == 1) { %>
                    <% informationRetrieval.rankedIndexTrendyol(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 5 && type == 2) { %>
                    <% informationRetrieval.positionalIndexTrendyol(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 5 && type == 3) { %>
                    <% informationRetrieval.invertedIndexTrendyol(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 6 && type == 1) { %>
                    <% informationRetrieval.rankedIndexGittigidiyor(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 6 && type == 2) { %>
                    <% informationRetrieval.positionalIndexGittigidiyor(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <% if (platform == 6 && type == 3) { %>
                    <% informationRetrieval.invertedIndexGittigidiyor(value).forEach(each => { %>
                        <div class="w3-col l3 m6 w3-margin-bottom">
                        <img src="" alt="Not Found" style="width:100%">
                        <% if(each.length > 1) %>
                        <h3 class="title"><%= each[1] %></h3>
                        <% if(each.length > 0) %>
                        <p class="w3-opacity hierarchy"><%= each[0] %></p>
                        <% if(each.length > 2) %>
                        <p class="description"><%= each[2] %></p>
                        </div>
                    <% }) %>
                <% } %>
                <p><button class="w3-button w3-light-grey w3-block">Load More</button></p>
            <% } %>